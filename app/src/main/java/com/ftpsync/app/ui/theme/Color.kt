package com.ftpsync.app.ui.theme

import androidx.compose.ui.graphics.Color

// Japandi Background & Surface
val JapandiBg = Color(0xFFF4F0EA)      // Warm cream/oat background
val JapandiSurface = Color(0xFFFAF8F5) // Soft natural off-white surface
val LinenBorder = Color(0xFFDFDCD6)    // Thin natural linen gray border

// Text
val DarkCharcoal = Color(0xFF2D2E2C)   // Muted charcoal instead of pure black
val SandMuted = Color(0xFF8E877F)      // Warm sand/taupe for secondary text

// Sage & Forest (Active State)
val ForestGreen = Color(0xFF4F6456)    // Grounded organic green
val SageLight = Color(0xFFE4EBE6)      // Sage/moss light background

// Clay & Terracotta (Inactive State)
val ClayAccent = Color(0xFFB37E67)     // Warm clay/terracotta
val ClayLight = Color(0xFFF5ECE9)      // Very soft clay light background

// Crimson & Terracotta Warning (Error State)
val CrimsonWarning = Color(0xFFC05C4E) // Soft terracotta warning red
val CrimsonLight = Color(0xFFF9EAE8)   // Soft light warning background
